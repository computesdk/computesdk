# URL of the deployed bridge Worker, for example:
# https://cloudflare-sandbox-bridge.<your-subdomain>.workers.dev
CLOUDFLARE_SANDBOX_URL=

# Same value as the bridge Worker's SANDBOX_API_KEY secret.
CLOUDFLARE_SANDBOX_API_KEY=
