{
  "name": "@computesdk/example-cloudflare-direct",
  "version": "0.0.7",
  "private": true,
  "description": "ComputeSDK Cloudflare direct mode Worker example",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@computesdk/cloudflare": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "^4.135.0"
  }
}
