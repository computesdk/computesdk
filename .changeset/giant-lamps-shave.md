---
"@computesdk/modal": minor
---

add ports to modal provider config and fix filesystem timeout issues by removing problematic sandbox.open() API calls and adding explicit timeouts to all exec() operations
