---
"@computesdk/cloud-run": patch
---

Add an explicit Cloud Run execution mode option with ephemeral `sandbox do` execution as the default and opt-in stateful `sandbox run`/`exec`/`delete` sessions.
