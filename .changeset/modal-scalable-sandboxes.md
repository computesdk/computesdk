---
"@computesdk/modal": minor
---

Add `scalableSandboxes` option to the Modal provider, enabling Modal's experimental scalable sandbox creation. Configurable via provider config or per `create()` call.
