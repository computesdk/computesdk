---
"@computesdk/browseruse": minor
"@computesdk/steel": minor
---

Add `@computesdk/browseruse` and `@computesdk/steel` browser providers for cloud browser sessions with stealth mode, proxies, profiles, and session recording. Both expose Playwright-compatible CDP endpoints.
