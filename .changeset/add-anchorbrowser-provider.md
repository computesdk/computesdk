---
"@computesdk/anchorbrowser": minor
---

Add `@computesdk/anchorbrowser` browser provider for cloud browser sessions with stealth mode, proxies, profiles, and session recording. Exposes a Playwright-compatible CDP endpoint.
