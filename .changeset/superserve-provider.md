---
'@computesdk/superserve': minor
---

Add the initial Superserve provider package. Superserve provides sandbox infrastructure to run code in isolated cloud environments powered by Firecracker MicroVMs. Wraps `@superserve/sdk` to expose sandbox lifecycle (create, connect, list, destroy), command execution, filesystem operations, and template listing through the standard ComputeSDK provider interface.
