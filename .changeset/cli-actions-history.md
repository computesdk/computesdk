---
'@computesdk/cli': patch
---

`compute actions history <repo> --workflow <path|name>` — recent run history with per-job/step failure rates (the flakiness signal from `GET /api/v1/actions/history`), with `--branch`, `--job`, `--limit`, and `--json`.
